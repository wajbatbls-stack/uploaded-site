# ملاحظات تنفيذ إدارة الخدمات

- مصدر الخدمات الحالي هو مجموعة المحتوى `services` في `contentCollections`، وتُنشأ القيم الأصلية من `server/siteSeed.ts`. يتكون كل سجل أصلي من عنوان ورمز تعبيري وترتيب وحالة ظهور ومصفوفة `items` للخدمات الفرعية.
- لا يوجد نموذج بيانات مستقل للخدمات بعد؛ لذلك ستُحافظ التوسعة على عناصر المجموعة القائمة وتضيف خصائص هرمية وتصميمية متوافقة معها بدلاً من استبدالها أو إنشاء بيانات بديلة.
- ستبقى ملفات الصور والأيقونات في التخزين المخصص، بينما تحفظ الخدمات مراجعها فقط.
- كل إجراءات التعديل ستكون محمية بجلسة المالك، أما القراءة للزوار فستقتصر على العناصر الظاهرة فقط.

لا تحتاج التوسعة إلى استبدال جدول المحتوى القائم: ستُضاف حقول متوافقة إلى كل سجل داخل محتوى `services`، مع إعداد قسم مركزي داخل إعدادات الموقع. يحافظ ذلك على جميع العناوين والخدمات الفرعية الأصلية عند الترحيل.

## التحقق البصري — 13 أغسطس 2026

- بقيت واجهة الزائر الافتراضية كما هي على سطح المكتب والهاتف بعد تحميل أصول الخدمات الجديدة.
- شاشة دخول المالك ظلت قابلة للاستخدام على الهاتف ولم تتأثر بإضافة مدير الخدمات.
- أكد فحص الاتصال القرائي المحمي وجود 17 خدمة أصلية في مجموعة الخدمات، ولم يكتب الفحص أي بيانات.
