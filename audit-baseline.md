# تدقيق مرجع واجهة الزائر

- **المرجع:** `/home/ubuntu/upload/project(1).zip`، مستخرج للفحص فقط في `/home/ubuntu/original-site-baseline`.
- **نتيجة تطابق مؤكدة:** الملفان `assets/css/style.css` و`pages/content.js` في المشروع الحالي متطابقان حرفياً مع الحزمة الأصلية (SHA-256 متساوٍ في 2026-08-13).
- **نتيجة أولية:** أي اختلاف محتمل في واجهة الزائر يأتي من طبقات الإدارة الديناميكية فقط (`site-design-r2.css`، `homepage-manager-r1.css`، `services-manager-r2.css`، و`site-app-r10.js`) أو من محتوى الإعدادات المحفوظة، لا من ملف التصميم أو المحتوى الأصليين.
- **ملاحظة تشغيلية:** محاولة فتح النسخة الأصلية مباشرة عبر `file://` لم تعرض التطبيق لأن مسارات الوحدات والموارد تتطلب خادماً HTTP؛ لم يُعدّل المرجع.

## مقارنة تشغيلية لصفحة الخدمات

تمت المقارنة في 2026-08-13 بين `http://127.0.0.1:4173/#/services` للمرجع الأصلي و`https://3000-ih9gslz595o15f45k6ivh-80b5c939.us2.manus.computer/?from_webdev=1#/services` للنسخة الحالية. احتفظ المرجع بعنوان القسم **«خدماتنا الأكاديمية»** ونصه **«اختر القسم المطلوب لعرض الخدمات الفرعية المتاحة»** وبشبكة خمس بطاقات في الصف. النسخة الحالية استبدلت النص بـ **«اختر الخدمة المناسبة وابدأ طلبك بكل سهولة.»** وحولت الشبكة إلى بطاقات بعرض صف كامل، وأدخلت لون بطاقة مخصص وصورة على الخدمة الأخيرة.

المرجع يستخدم شعار CloudFront الأصلي ورقم واتساب واحداً `966567680470` وتسلسل تنقل يضع «الخدمات» بعد الرئيسية. النسخة الحالية كانت تستخدم شعاراً محفوظاً داخل تخزين الإدارة، ورابط واتساب مدمجاً بصورة غير سليمة، ونصاً إضافياً في الشريط، وتنقل الخدمات في موضع متأخر. مصدر هذه الفروقات هو سجلا `siteSettings` و`services` المحفوظان في قاعدة البيانات؛ المرجع الافتراضي موجود في `server/siteSeed.ts` (الخدمات في السطور 3–199 وإعدادات الموقع في السطور 816–828).

## الاستعادة والتحقق

في 2026-08-13 استُعيدت مجموعتا `services` و`siteSettings` إلى مرجع `SITE_SEED` عبر استعلامات مقيّدة. ثم فُتح المسار `/#/services` وتحقق العرض من العنوان والوصف والخدمات الخمس عشرة بالترتيب المرجعي، والهوية وبيانات الاتصال الأصلية. لم تسجل وحدة التحكم أي خطأ JavaScript أثناء الفحص.
