# اختبار وظيفي فعلي لروابط الزوار

## بيئة الاختبار

- لوحة المالك المنشورة: `https://uploadplus-47dkogbk.manus.space/admin?functional-test=visitor-links`
- تاريخ الاختبار: 14 أغسطس 2026.
- الهدف: إثبات إنشاء رابط زائر فعلياً وحفظه وفتحه كزائر ثم تنظيفه، لا الاكتفاء بفحص الواجهة.

## الحالة قبل الإنشاء

- فُتح قسم **إنشاء روابط الزوار** من لوحة المالك المنشورة بنجاح.
- ظهرت إحصاءات الروابط المحفوظة، وزر **إنشاء رابط زائر جديد**، وبطاقة رابط موجود.
- الرابط الموجود يعرض حالة نشطة، عنوان URL عام، عدد زيارات، وأزرار النسخ وواتساب وتيليجرام والمعاينة والاختبار كزائر والتعديل والتعطيل والحذف.
- سيُنشأ سجل اختبار يحمل اسماً مميزاً ثم يُحذف بعد التحقق.

## محاولة الإنشاء

فُتح نموذج الإنشاء وأُدخل الاسم المميز `اختبار وظيفي مؤقت — يُحذف بعد التحقق 20260814-1235`. بعد الضغط على زر **إنشاء الرابط والتحقق منه** بقي النموذج ظاهراً في لقطة الاستجابة الأولى ولم تظهر بعد بطاقة نجاح أو رسالة خطأ. ستتم قراءة حالة الصفحة ووحدة التحكم في الخطوة التالية لتحديد هل الطلب ما زال قيد التنفيذ أم رفضه الخادم أو تعطل معالج الحفظ.

## دليل التنفيذ الفعلي

أكد استعلام قاعدة البيانات حفظ سجل فعلي بالمعرّف `150001` والرمز `bcee205553a844e5ae7613277adf504dd16baa16`، مع الوجهة `/` والحالة النشطة. فُتح الرابط العام التالي كزائر: `https://uploadplus-47dkogbk.manus.space/?link=bcee205553a844e5ae7613277adf504dd16baa16`، وتم تحميل واجهة الزائر. بعد ذلك أكّد الاستعلام نفسه ارتفاع `visitCount` من `0` إلى `1` وتعيين `lastVisitedAt` إلى `2026-08-14 12:37:16`، ما يثبت أن الإنشاء والحفظ والحلّ وتسجيل الزيارة تعمل عبر قاعدة البيانات. هذا السجل مؤقت وسيُحذف بعد التحقق من ظهوره وإدارته في لوحة المالك.

## الظهور والتنظيف

ظهر سجل الاختبار فعلياً في قسم **إنشاء روابط الزوار** داخل لوحة المالك، مع حالته النشطة، الرابط الكامل، عدد الزيارات `1`، وآخر استخدام، وأزرار النسخ وواتساب وتيليجرام والمعاينة والاختبار كزائر والتعديل والتعطيل والحذف. بعد ذلك حُذف السجل المؤقت بقيد دقيق يجمع المعرّف والرمز والاسم. أكّد استعلام تحقق لاحق أن عدد السجلات المطابقة أصبح `0`، ولذلك لم يتبقَ أي رابط اختبار أو تغيير في الروابط الحقيقية.

> النتيجة: القسم ليس واجهة شكلية؛ الاختبار أثبت إنشاء سجل فعلي في قاعدة البيانات، وحلّه في مسار الزائر، وتسجيل زيارته، وظهوره في الإدارة، ثم تنظيفه بأمان.
