# توثيق التحقق من الإنتاج — 12 أغسطس 2026

## نموذج تسليم الواجب

تم فتح نسخة الإنتاج على المسار `#/assignment` مع مرجع الإصدار `release=20260812-r4`. بعد تعبئة الحقول الإلزامية واستخدام وصف قصير (`بني`)، أعاد معالج النموذج منع الإرسال محلياً قبل أي طلب حفظ للخادم وأظهر التنبيه العربي التالي:

> **أكمل وصف الواجب** — يرجى كتابة وصف الواجب بالتفصيل، بما لا يقل عن 8 أحرف.

وبذلك لا يظهر كائن التحقق الخام الخاص بالخادم للزائر، ولا ينشأ سجل طلب من محاولة الوصف القصير.

## إحصاءات الزائر

حزمة الواجهة التي بُنيت للإصدار الحالي لا تتضمن قسم «أرقامنا تتحدث» أو بطاقات أرقام الطلاب والخدمات والرضا والدعم. الإحصاءات التشغيلية تبقى مخصصة للوحة المالك.

## لوحة المالك

تم فتح الرابط المنشور `/admin?release=20260812-r4` بعد التحقق من الجلسة الخادمية. ظهرت لوحة الإدارة الحديثة، بما يشمل وحدات الخدمات والاشتراكات والتحميلات والمدونة والآراء والشركاء والأسئلة الشائعة وطلبات العملاء والرسائل والتقييمات والوسائط وإعدادات الموقع والحساب والنسخ الاحتياطي. لا يظهر رابط هذه اللوحة في واجهة الزائر.

## طلب زائر حقيقي

بعد تأكيد المستخدم إرساله طلباً من رابط الزائر، ظهر في قاعدة البيانات طلب واحد جديد بالمعرّف `1` وحالة `new`. كما ظهرت بطاقة «طلبات العملاء 1» في لوحة المالك، وظهر تنبيه اللوحة «طلبات محفوظة: 1»، وتغير ملخص الحالات إلى «جديد: 1». هذا يثبت أن إرسال النموذج يحفظ الطلب الحقيقي ويصل إلى وحدة الإدارة والتنبيهات.

## تحديث الحالة

فُتحت وحدة «طلبات العملاء» في لوحة المالك، وظهر الطلب الحقيقي بالمعرّف `1` مع تفاصيله والمرفق المحفوظ. تم تغيير حالته عبر واجهة الإدارة من `جديد` إلى `قيد المراجعة` وحفظ التعديل. تحقق الاستعلام اللاحق من قاعدة البيانات أن قيمة الحالة أصبحت `reviewing` عند `2026-08-12 02:59:01`، ما يؤكد استمرار الربط بين لوحة المالك وبيانات الطلب.
